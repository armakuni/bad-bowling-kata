on:
  push:
    paths:
      - .github/workflows/javascript.yml
      - ./javascript/*

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Deps
        run: npm install

      - name: Run Tests
        run: npm run test

      - name: Run Mutation Tests
        run: npm run mutate